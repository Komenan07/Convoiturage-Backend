# 📁 Dossier des dépendances
node_modules/

# 🧪 Fichiers de logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 📂 Dossiers temporaires
tmp/
temp/

# 🧠 Fichiers de configuration système
.DS_Store
Thumbs.db

# 🔐 Fichiers sensibles ou secrets
.env
.env.local
.env.*.local

# 🧪 Résultats de tests ou coverage
coverage/
*.lcov

# 🛠️ Fichiers de build ou de transpilation
dist/
build/

# 📦 Fichiers de package lock (optionnel selon ton workflow)
package-lock.json
yarn.lock
pnpm-lock.yaml
test

# Firebase
config/serviceAccountKey.json

# Docker
docker-compose.override.yml
.env.staging
.env.production

# Backups
backups/*.gz

# Uploads (ne pas versionner les fichiers uploadés)
uploads/photos/*
uploads/documents/*
!uploads/photos/.gitkeep
!uploads/documents/.gitkeep